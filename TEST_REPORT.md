# r1.1.19 fix8 test report
- JavaScript syntax: PASS
- Decoration change handling: single authoritative select onchange; writes `build.decos[key]` before calculation; duplicate workspace change listener removed.
- Armor card spacing: substantially reduced, including card padding, slot row padding/gap, and inter-card gap.
- Calculation functions otherwise preserved from fix7.
