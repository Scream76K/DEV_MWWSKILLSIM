# v7.8.0 OCR precision

## 目的
v7.7.x の自動判定で「画面全体の二値化」が逆にOCRノイズを増やすケースが確認されたため、通常の自動判定経路を再調整。

## 主な変更
- 自動判定の一次OCRは、二値化せず元画像の色を保持して認識。
- Tesseractの座標付き行を利用し、行単位で候補照合。
- 装備名候補はカテゴリ別の強い条件を通してからMHDB照合。
- 装飾品は「珠」を含む行を中心に照合し、低信頼候補を抑制。
- OCR全文とは別に「候補抽出に使用した行」を表示し、ノイズと候補を区別。
- 既存の確認UI（1位を初期表示、2/3位、該当なし）を維持。
- 護石スロット「防2-防1」の修正内容を維持。
- 友人向けベータ版 MHWSKILLSIM は変更しない。
