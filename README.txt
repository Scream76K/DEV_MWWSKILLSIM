MH Wilds OCR — アイコン起点テスト v0.8

v0.7で発見されたRARE判定呼び出し漏れを修正。
detect() → enrichRare() → render() の順に変更。

RARE色判定はHSVベース:
- 色相(H)を主判定
- 彩度(S)と明度(V)は許容幅を持たせる
- RARE 3/4、6/7は近い場合に曖昧表示
- 判定結果にサンプルRGB平均も表示し、実画面との色ズレを調整できるようにした
