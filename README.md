# MHWilds OCR v4.5.9

装飾品Lvの扱いを修正。

- 装備DBのスロットは「装着可能な上限」であり、実際の装飾品Lvを拘束しない。
- OCRで読み取った装飾品の実Lvを優先する。
- DB候補は装備スロット上限を超えないものだけを候補にする。
- 実LvとDB候補のslotが一致した場合に照合スコアを加点する。
- 表示を「期待Lv」から「上限Lv」に変更。
- OCR実Lvが上限を超えた場合のみ警告。
- 装備名OCR、装飾品グループ検出、限界突破後スロット算出は変更しない。
