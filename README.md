# MHWilds OCR v4.5.11

v4.5.10をベースに、装備OCRのα/β/γ型式判定だけを修正。

## 修正内容
- OCRで `B6` / `BQ` / `bq` / `80q` など、βが誤認識された末尾表記をβとして認識。
- シリーズ名が同一でα/βが同点になるケースでも、型式OCRの一致を候補選択へ反映。
- 装飾品OCR、グループ構造、ビルド反映UI、既存の装備OCR前処理は変更しない。
- スロット上限は装飾品Lvの上限としてのみ扱い、装飾品Lvを上限値に強制しない。
