# OCR Step 1.3

メイン武器だけを対象に、5種類の切り出し変形 × 5種類の前処理/OCRを行い、MHDB候補との類似度・支持回数・OCR信頼度を統合します。

## 重要仕様
- 上段のメイン武器のみ。下段サブ武器は対象外。
- 「最も近い辞書名」を無条件に正解にしない。
- 複数OCRの合意と候補間マージンが不足した場合は判定保留。
- 現版は診断用の小辞書。将来MHDBがロードされていれば `DB.weapons` を優先する接続点を用意。
- シミュレータ本体への自動反映はまだ行わない。
